# 8. Write a program to create a list of the cubes of only the even integers
# a. Using 'for' loop
# b. Using list comprehension
lst = [1, 2, "hi", 4, 5, 6]

cubes = []
for i in lst:
    if type(i) == int and i % 2 == 0:
        cubes.append(i**3)
print("Using loop:", cubes)

cubes2 = [i**3 for i in lst if type(i) == int and i % 2 == 0]
print("Using comprehension:", cubes2)
